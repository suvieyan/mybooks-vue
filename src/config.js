// const host = 'http://localhost:5757'
// 开发环境
// const host = 'https://q0l7sxhs.qcloud.la'

const host = 'http://127.0.0.1:5000'

const config = {
  host,
  loginUrl: `${host}/login`,
  uploadUrl: `${host}/upload`
}
export default config
